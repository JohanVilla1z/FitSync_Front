//TODO: Create a Profile page that displays the user's profile information. This page should be protected and only accessible to authenticated users.
const Profile = () => {
  return <div>Profile placeholder</div>;
};

export default Profile;
